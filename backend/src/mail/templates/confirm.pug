doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Confirmation
    link(href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet")
    style.
      :root {
        --main-bg: #fff;
        --second-bg: #c6e0ff;
        --select-bg: #92c7ff;
        --block-bg: #d1e6ff;
        --border: #002049;
        --main-text: #000;
        --second-text: #1f4269;
        --select-text: #043457;
      }
      body {
        font-family: 'Inter', Arial, sans-serif;
        color: var(--main-text);
        line-height: 1.6;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        background-color: var(--main-bg);
      }
      .container {
        background-color: var(--main-bg);
        padding: 20px;
        border-radius: 15px;
        border: 1px solid var(--border);
      }
      .header {
        text-align: center;
        margin-bottom: 20px;
      }
      h1 {
        color: var(--second-text);
        font-size: 24px;
        font-weight: 700;
      }
      p {
        margin-bottom: 15px;
        color: var(--main-text);
      }
      .verify-button {
        display: inline-block;
        white-space: nowrap;
        font-weight: 700;
        padding: 12px 20px;
        background-color: var(--block-bg);
        border: 1px solid var(--border);
        border-radius: 15px;
        text-decoration: none;
        color: var(--main-text);
        margin: 20px 0;
        transition: background-color 0.2s ease, color 0.2s ease;
      }
      .verify-button:hover {
        background-color: var(--select-bg);
        color: var(--select-text);
      }
      .footer {
        margin-top: 30px;
        text-align: center;
        font-size: 14px;
        color: var(--second-text);
      }
  body
    .container
      .header
        h1 Welcome to Weather Updates!

      p Hello,

      p Thank you for subscribing to our weather notification service! To complete your subscription, please click the button below:

      p(style="text-align: center")
        a(href=confirmUrl class="verify-button") Confirm Subscription

      p If you didn't request this subscription, you can safely ignore this email. No action is required on your part.

      .footer
        p Best regards,
        p The Weather Team